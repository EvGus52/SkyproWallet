import {
  AuthContainer,
  AuthCard,
  AuthTitle,
  AuthForm,
  FormGroup,
  InputWrapper,
  Input,
  SubmitButton,
  ErrorMessage,
  AuthLink,
} from "../../components/common/AuthFormStyles";

// Экспортируем общие компоненты с локальными именами
export const LoginContainer = AuthContainer;
export const LoginCard = AuthCard;
export const LoginTitle = AuthTitle;
export const LoginForm = AuthForm;
export { FormGroup, InputWrapper, Input, ErrorMessage };

export const LoginButton = SubmitButton;
export const RegisterLink = AuthLink;
